#ifndef SHORTESTPATH_H

    #define SHORTESTPATH_H

    #include "pair.h"
    #include "graph.h"

    /**
     * @brief 
     * 
     * @param g 
     * @return pair* 
     */
    pair_t* all_pairs_shortest_path(graph_t* g);

#endif